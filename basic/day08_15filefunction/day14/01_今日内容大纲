一. 上次课内容回顾
    1. 生成器, 本质就是迭代器. 获取生成器的办法: 生成器函数, 生成器表达式, 通过数据转化
    2. 生成器函数:
        在函数内部有yield
        这个时候我们执行这个函数. 这个时候是获取生成器
        __next__()可以触发生成器的执行, 执行到下一个yield.
        如果执行完最后一个yield之后. 继续__next__(), 程序会报错StopIteration

        send(), 可以给上一个yield传值. 执行到下一个yield.
        不能用在开头或者结尾

        特点:
            1. 惰性机制
            2. 只能向前
            3. 节省内存
    3. 推导式
        1. 列表推导式
            [结果 for 判断]
        2. 字典推导式
            {k:v for 判断}
        3. 集合推导式
            {k for 判断}
    4. 生成器表达式
        (结果 for  判断)

        三元运算符
        a if a > b else b

    没有元组推导式


二. 作业讲解
三. 今日主要内容
    1. 内置函数
    预习: lambda(匿名函数), sorted(), filter(), map(), 递归(贪吃蛇), 二分法